// כתובת בסיס לשרת ה-API/Socket.io.
// בפריסה בענן (שירות Railway אחד), הלקוח מוגש מאותו שרת בדיוק - אז מחרוזת ריקה (יחסי) עובדת מושלם.
// בפיתוח מקומי, ה-client רץ על פורט נפרד (Vite) מה-server - אז צריך כתובת מלאה.
export const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:5000';
